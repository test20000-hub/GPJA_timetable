# 군포중앙고등학교 시간표

NEIS 고등학교시간표를 이용하는 군포중앙고 전용 모바일 시간표/PWA입니다.

## 구성

- GitHub Pages 정적 웹앱
- GitHub Actions가 NEIS API에서 시간표를 주기적으로 수집
- NEIS API 키는 `NEIS_API_KEY` GitHub Actions Secret으로만 사용
- 학년/반 선택
- 주간 시간표 및 오늘 시간표
- PWA 설치 및 마지막 데이터 오프라인 캐시
- 급식 조회 및 메뉴 검색
- 학사일정 조회
- 다크모드 및 Liquid Glass 스타일 UI
- 시스템 라이트/다크 모드 자동 반응형 표시
- 모바일 앱 및 홈 화면 위젯 지원
- 최초 접속 시 사이트 비밀번호 인증 및 이후 자동 인증 유지
- Android 앱의 관리자 기능 활성화 및 QR 기기 승인
- 사이트 로그인 후 어디서든 `QR 승인 센터`로 이동할 수 있는 고정 바로가기

학교 정보: 시도교육청 코드 `J10`, 학교 코드 `7531272`.

## 버전 기록

### v1.6.3
- Android 위젯 자동 새로고침을 1분 주기 AlarmManager 방식으로 안정화
- 위젯의 현재 교시/다음 교시 판정 로직 수정
- 쉬는 시간에 잘못된 교시가 표시되던 문제 수정
- 시간표 날짜 판정을 한국 표준시(Asia/Seoul) 기준으로 통일
- Android 앱 versionCode `17`, versionName `1.6.3` 반영


### v1.6.2
- 모바일 하단 네비게이션과 `QR 승인 센터` 고정 바로가기의 겹침 문제 수정
- 모바일에서는 QR 승인 센터 바로가기를 하단 네비게이션 위쪽으로 자동 배치
- QR 승인 센터의 정적 버전 표기 및 캐시 버전 갱신

### v1.6.1
- 로그인 후 일반 시간표 화면에서 QR 승인 센터로 이동할 위치가 없던 문제 수정
- 모든 일반 페이지에서 화면 오른쪽 아래 `QR 승인 센터` 고정 바로가기 제공
- 기존 비밀번호 인증 및 관리자/일반 앱 기능은 그대로 유지
- 사이트 인증 스크립트 버전 `1.6.1` 반영

### v1.6.0
- 관리자 기능을 일반 앱과 분리된 역할로 취급하도록 Android 앱 구조 개선
- 관리자 기능이 활성화된 기기도 일반 시간표·급식·학사일정 등 일반 앱 기능을 그대로 사용하도록 변경
- 기존 v1.5.9의 단일 `role=admin` 저장 방식을 `admin_enabled` 기반의 병행 권한 구조로 변경
- 기존 관리자 앱 설치본은 v1.6.0에서 자동으로 병행 관리자 기능 상태로 마이그레이션
- 일반 앱 사용 중에도 QR 승인 센터에서 `관리자 기능 활성화`를 선택해 `ADMIN_CODE`로 관리자 기능을 추가할 수 있도록 변경
- 관리자 기능 활성화 상태에서는 관리자 승인 QR 스캔/생성과 일반 앱 사용을 동시에 지원
- Android 앱 versionCode `14`, versionName `1.6.0` 반영
- GitHub Actions APK 릴리스 설정 `v1.6.0` 반영
- 사이트 및 QR 승인 센터 버전 `v1.6.0` 동기화

> 보안 참고: `ADMIN_CODE`를 APK에 빌드타임으로 주입하면 저장소에는 노출되지 않지만, APK 자체를 분석할 수 있는 사용자는 코드 추출을 시도할 수 있습니다. 따라서 이는 무단 관리자 등록을 크게 줄이는 장치이며, 고보안 인증을 위해서는 서버 측 인증 또는 하드웨어 키 기반 인증이 필요합니다.

### v1.5.9
- Android 앱 최초 실행 시 `관리자 앱` 선택만으로 관리자 권한을 얻을 수 있던 문제 수정
- 관리자 앱 등록 전에 `ADMIN_CODE` GitHub Actions Secret과 일치하는 관리자 등록코드 입력을 요구하도록 변경
- 관리자 등록코드는 저장소 소스 코드에 직접 기록하지 않고 GitHub Actions Secret에서 APK 빌드 시 주입
- 기존 v1.5.8 관리자 선택 상태는 v1.5.9에서 관리자 인증 버전 검사를 거쳐 다시 등록코드를 요구하도록 변경
- 일반 앱은 기존처럼 등록코드 없이 사용할 수 있도록 유지
- Android 앱 versionCode `13`, versionName `1.5.9` 반영
- GitHub Actions APK 릴리스 설정 `v1.5.9` 반영
- 사이트 주요 페이지 풋터 버전 `v1.5.9` 동기화

### v1.5.8
- GitHub Pages만 사용하는 구조에 맞춘 QR 기기 승인 센터 추가
- Android 앱 최초 실행 시 `관리자 앱` 또는 `일반 앱` 역할 지정
- 관리자 앱에서 승인 요청 QR을 스캔하여 승인 QR 생성
- 일반 앱에서 관리자 키 QR 등록 → 승인 요청 QR 생성 → 승인 QR 스캔 순서 지원
- Android WebView와 GitHub Pages 사이의 승인 상태 브리지 추가
- 승인된 일반 Android 앱은 사이트 비밀번호와 별도로 관리자 승인이 있어야 접근하도록 적용
- QR 승인 센터에 카메라 스캔 UI 및 Liquid Glass 대응 UI 추가
- Android 카메라 권한 및 WebView 카메라 접근 처리 추가
- Android 앱 versionCode `12`, versionName `1.5.8` 반영
- GitHub Actions APK 릴리스 설정 `v1.5.8` 반영
- 사이트 풋터 버전 `v1.5.8` 동기화

### v1.5.7
- 시스템의 라이트/다크 디스플레이 모드를 자동 감지하도록 개선
- 기기 설정이 변경되면 수동 테마를 선택하지 않은 경우 사이트 테마도 즉시 동기화
- 브라우저 `theme-color`도 자동 변경
- 시간표·급식·학사일정 페이지의 테마 버튼 상태를 동기화
- 사이트 풋터 버전 `v1.5.7` 반영

### v1.5.6
- Android WebView에서 다른 탭으로 시간표 페이지를 이동할 때 PWA 전용 스플래시가 매번 표시되던 문제 수정
- Android WebView에서는 PWA 스플래시를 표시하지 않고 즉시 시간표 화면을 표시하도록 수정
- Android 앱 버전 `1.5.6` 및 versionCode `11` 반영
- Android APK 릴리스 빌드 설정 `v1.5.6` 반영
- 시간표 페이지 풋터 버전 `v1.5.6` 반영

### v1.5.5
- 사이트 전체 페이지에 비밀번호 인증 시스템 추가
- 최초 접속 시 비밀번호 입력 후 입장
- 인증 성공 시 브라우저 `localStorage`에 인증 상태를 저장하여 다음 접속부터 자동 입장
- 비밀번호 입력 화면에 학교 로고 및 다크모드 대응 UI 적용
- 시간표·급식·학사일정 페이지 모두 인증 적용
- 사이트 풋터 버전 `v1.5.5` 반영

### v1.5.4
- 급식 날짜 이동에서 토요일·일요일 자동 건너뛰기
- 주말에 앱을 열거나 오늘 버튼을 눌렀을 때 다음 평일로 자동 이동
- 급식 메뉴 항목을 올바른 버튼 구조로 수정하여 메뉴 검색 동작 안정화
- 급식 메뉴 클릭 시 Google에서 해당 메뉴 검색
- 급식 페이지 캐시 버전 갱신
- 급식 페이지 풋터 버전 `v1.5.4` 반영

### v1.5.3
- 사이트 주요 페이지의 버전 표기 통일
- `index.html`, `meal.html`, `schedule.html` 풋터 버전 `v1.5.3` 반영
- 모바일 앱/사이트 UI 개선 사항 반영

### v1.5.2
- 급식 페이지 UI 및 기능 개선
- 급식 정보 표시 및 날짜 탐색 개선
- 급식 페이지 버전 `v1.5.2` 반영

### v1.5.0
- Android 앱 버전 `1.5.0`으로 통일
- Android 위젯 및 앱 빌드 구성 개선
- GitHub Actions Android APK 빌드/릴리스 구성 개선
- `GPJA-Timetable-v1.5.0.apk` 릴리스
- 앱 아이콘 및 학교 로고 적용
- 앱 실행 화면 및 시스템 바 영역 처리 개선

### v1.3.3
- Android 앱 빌드 및 배포 안정화
- 위젯 크기 및 표시 개선
- 앱/위젯 관련 Android 구성 업데이트

### v1.3.1
- Android 앱의 시스템 상태바와 WebView 콘텐츠 겹침 문제 수정
- 시스템 바 인셋을 WebView에 적용
- Android 빌드 설정 개선

### v1.2.2
- 학사일정 페이지 버전 `v1.2.2` 반영
- 학사일정 데이터 표시 개선

### v1.0.0 ~ v1.2.1
- 군포중앙고등학교 시간표 웹앱 기본 기능 구축
- NEIS 시간표 데이터 연동
- 학년·반 선택 기능
- 주간 시간표 및 오늘 시간표 표시
- PWA 설치 지원
- 오프라인 캐시 및 서비스 워커 적용
- 급식/학사일정 페이지 추가 및 초기 UI 구축
- 모바일 화면 대응 및 기본 네비게이션 구축

> 버전 기록은 저장소에 반영된 주요 기능·UI·앱 업데이트를 기준으로 정리합니다.

## 현재 버전

**사이트: v1.6.3**  
**Android 앱: v1.6.3**

## 최초 설정

1. 저장소의 **Settings → Secrets and variables → Actions**에서 `NEIS_API_KEY`라는 Repository secret을 만들고 NEIS 인증키를 저장합니다.
2. 같은 위치에 `ADMIN_CODE`라는 Repository secret을 추가하고 관리자 앱 등록에 사용할 비밀 코드를 저장합니다.
3. **Settings → Pages**에서 Source를 **GitHub Actions**로 설정합니다.
4. **Actions → Update timetable and deploy Pages → Run workflow**를 실행합니다.
5. Android 앱은 `Build Android Widget` 워크플로에서 `ADMIN_CODE`를 빌드타임에 주입하여 APK를 생성합니다.

NEIS 공식 개발자 가이드에 따라 Open API 인증키가 필요합니다. 고등학교시간표 데이터셋은 매일 적재되며 2025학년도 이후 데이터는 기존 방식으로 사용할 수 있습니다.
