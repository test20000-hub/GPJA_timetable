(()=>{if(innerWidth>700)return;const pages=['index.html','meal.html','schedule.html'],bar=document.querySelector('.app-tabs');if(!bar)return;const links=[...bar.querySelectorAll('.app-tab')];const path=location.pathname.split('/').pop()||'index.html';const current=Math.max(0,pages.indexOf(path));let sx=0,sy=0,drag=false;const setPreview=dx=>{const amount=Math.max(-1,Math.min(1,dx/120));links.forEach((el,n)=>{const distance=n-current;el.style.transform=`translateX(${amount*-distance*10}px)`})};bar.addEventListener('touchstart',e=>{const t=e.changedTouches[0];sx=t.clientX;sy=t.clientY;drag=true},{passive:true});bar.addEventListener('touchmove',e=>{if(!drag)return;const t=e.changedTouches[0],dx=t.clientX-sx,dy=t.clientY-sy;if(Math.abs(dx)>Math.abs(dy))setPreview(dx)},{passive:true});bar.addEventListener('touchend',e=>{if(!drag)return;drag=false;const t=e.changedTouches[0],dx=t.clientX-sx,dy=t.clientY-sy;links.forEach(el=>el.style.transform='');if(Math.abs(dx)<55||Math.abs(dx)<=Math.abs(dy))return;const next=dx<0?current+1:current-1;if(next>=0&&next<pages.length)location.href=pages[next]},{passive:true});bar.addEventListener('touchcancel',()=>{drag=false;links.forEach(el=>el.style.transform='')},{passive:true})})();