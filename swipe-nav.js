(()=>{if(innerWidth>700)return;const pages=['index.html','meal.html','schedule.html'],bar=document.querySelector('.app-tabs');if(!bar)return;const links=[...bar.querySelectorAll('.app-tab')];const path=location.pathname.split('/').pop()||'index.html';const current=Math.max(0,pages.indexOf(path));bar.style.setProperty('--nav-index',current);let sx=0,sy=0,drag=false;bar.addEventListener('touchstart',e=>{const t=e.changedTouches[0];sx=t.clientX;sy=t.clientY;drag=true;bar.classList.add('is-dragging')},{passive:true});bar.addEventListener('touchmove',e=>{if(!drag)return;const t=e.changedTouches[0],dx=t.clientX-sx,dy=t.clientY-sy;if(Math.abs(dx)<=Math.abs(dy))return;const width=bar.clientWidth/3,preview=Math.max(-1,Math.min(1,dx/width)),target=Math.max(0,Math.min(2,current-preview));bar.style.setProperty('--nav-index',target);},{passive:true});bar.addEventListener('touchend',e=>{if(!drag)return;drag=false;bar.classList.remove('is-dragging');const t=e.changedTouches[0],dx=t.clientX-sx,dy=t.clientY-sy;bar.style.setProperty('--nav-index',current);if(Math.abs(dx)<55||Math.abs(dx)<=Math.abs(dy))return;const next=dx<0?current+1:current-1;if(next>=0&&next<pages.length){bar.style.setProperty('--nav-index',next);setTimeout(()=>{location.href=pages[next]},140)}} ,{passive:true});bar.addEventListener('touchcancel',()=>{drag=false;bar.classList.remove('is-dragging');bar.style.setProperty('--nav-index',current)},{passive:true});links.forEach(a=>a.addEventListener('click',e=>{if(Math.abs(sx-(e.changedTouches?.[0]?.clientX||sx))>10)e.preventDefault()}))})();